name: simple_dart_logger
description: A simple yet extensible logger package written purely in dart. 
  It is purposefully platform agnostic and it doesn't use any dependencies.
version: 1.3.0
repository: https://github.com/MyLittlePenguin/simple_dart_logger

environment:
  sdk: ^3.0.6

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
